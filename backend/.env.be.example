JWT_SECRET=
JWT_ALGORITHM=
STRIPE_SECRET_KEY=

# Steps to Retrieve the Webhook Secret Key
# Log in to Stripe

# Visit the Stripe Dashboard and log in.
# Go to Webhooks

# Navigate to Developers > Webhooks in the left menu.
# Select or Create a Webhook Endpoint

# If you already have a webhook endpoint:
# Click on it to view its details.
# To create a new webhook:
# Click Add Endpoint.
# Enter your webhook URL (e.g., https://yourdomain.com/webhook). if you don't have a live url and want to test on local, use ngrok to format localhosts to some random but https url
# Select the events your app needs (e.g., checkout.session.completed).
# Retrieve the Signing Secret

# Scroll to the Signing secret section on the webhook details page.
# Click Reveal to display the webhook secret (e.g., whsec_xxx123abc456def).
# Store the Secret Securely

# Save the secret as an environment variable (e.g., STRIPE_WEBHOOK_SECRET) or in a secure location.

STRIPE_WEBHOOK_SECRET= 

SUPABASE_URL=
SUPABASE_ANON_KEY=



